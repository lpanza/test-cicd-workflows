apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: policy-generator-name
policyDefaults:
  namespace: policies
  placement:
    clusterSelectors:
      vendor: "OpenShift"
  remediationAction: enforce
policies:
  - name: goofy
    manifests:
      - path: manifest-file.yaml
  - name: donald
    manifests:
      - path: manifest-file2.yaml
